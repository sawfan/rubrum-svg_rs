[package]
name = "rubrum-svg"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
svg = "0.18.0"

# Backend-agnostic shared render/spec core.
rubrum-render = "*" 
rubrum-svg-helpers = "*" 

# Domain types + aspect computation.
rubrum = "*" 

