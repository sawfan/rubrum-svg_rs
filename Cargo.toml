[package]
name = "rubrum_svg"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
svg = "0.18.0"

# Backend-agnostic shared render/spec core.
rubrum_render = "*" 
rubrum_svg_helpers = "*" 

# Domain types + aspect computation.
rubrum = "*" 

